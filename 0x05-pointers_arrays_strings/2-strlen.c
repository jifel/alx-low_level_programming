#include "main.h"
/**
 * _strlen - returns the length of a string
 * @s : int paramter it points to
 * Return: int
 */

int _strlen(char *s)
{
	int len;
	char a;

	s = &a;

	len = strlen(s);

	return (len);
}
