#include "main.h"
/**
 * _strcpy - function that copies the string pointed to
 * @dest : paramter it receives
 * @src: parameter it receives
 * Return: char *
 */
char *_strcpy(char *dest, char *src)
{

	strcpy(src, dest);

	return (dest);
}
