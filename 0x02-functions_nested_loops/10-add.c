#include "main.h"
/**
 * add - entry point
 * @a: parameter 1
 * @b: parameter 2 the integer it receives
 * Descriptiion: function that adds two numbers
 * Return: always 0
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
