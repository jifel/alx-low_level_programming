#include "main.h"
#include <ctype.h>
/**
 * _abs - entry point
 * @int: accepts int value as parameter
 * Description: computes absolute value of an integer
 * Return: 0
 */
int _abs(int)
{
	int a, value;

	value = abs(a);
	_putchar(value);
	return (0);
}
