#include "main.h"
/**
 * _strcat - functions that concatenates two strings
 * @dest: parameter
 * @src: parameter
 * Return: pointer
 */
char *_strcat(char *dest, char *src)
{

	strcat(dest, src);

	return (dest);
}
